<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.LikeItemMapper">

    <!-- 관심물품 등록 -->
    <insert id="insertLikeItem" parameterType="list">
		INSERT INTO likeitem(no, memId, ScategoryCode)
		SELECT SEQ_LIKEITEM_NO.NEXTVAL no, T1.* FROM(
			<foreach collection="list" item="obj" separator="UNION ALL">
				SELECT '${obj.memId}' mem_id, '${obj.scategoryCode}' ScategoryCode FROM DUAL
			</foreach>
		) T1
    </insert>

</mapper>