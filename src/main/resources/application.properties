
# 서버번호 설정
server.port=5959

# context-path 설정
server.servlet.context-path=/SOBUN

# 뷰의 위치 설정 및 확장자 인식
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

spring.thymeleaf.servlet.produce-partial-output-while-processing=false

# DB 연동
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:tcp://1.234.5.158:31521/mini02;Mode=Oracle
spring.datasource.username=sa
spring.datasource.password=

# SQL문이 있는 위치 설정(mybatis)
# 이름이 Mapper로 끝나는 모든 xml 파일을 의미
mybatis.mapper-locations=classpath:/mappers/*/*Mapper.xml

# dto 위치 설정
mybatis.type-aliases-package=com.example.dto

# db로 세션관리
server.servlet.session.timeout=3600
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

# ddl 
# create  ->  엔티티의 정보를 읽어서 테이블 생성, 재구동 시 다시 생성(DROP + CREATE)
# update  ->  엔티티의 정보를 읽어서 변경사항 발생 시 생성(CREATE)
# none    ->  ddl을 사용하지 않음
# validate -> 엔티티와 테이블이 정상 매핑되었는지 확인 
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.hibernate.ddl-auto=update


# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sobun5959@gmail.com
spring.mail.password=ijfropuqdmsbvrtq
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.auth=true